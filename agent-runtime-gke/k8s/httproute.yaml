apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: currency-agent
spec:
  parentRefs:
    - name: currency-agent-gateway
  rules:
    - backendRefs:
        - name: currency-agent
          port: 80
